# Arvore Sintatica Atribuida

Arvore anotada com a categoria semantica, o operando/valor e o tipo inferido de cada no (resultado de `verificarTipos`).

- **Programa** : _DESCONHECIDO_
  - **Operador aritmetico** `+` : _REAL_ (linha 2)
    - **Literal numerico** `10.5` : _REAL_ (linha 2)
    - **Literal numerico** `2.5` : _REAL_ (linha 2)
  - **Operador aritmetico** `-` : _REAL_ (linha 3)
    - **Literal numerico** `50.0` : _REAL_ (linha 3)
    - **Literal numerico** `10.0` : _REAL_ (linha 3)
  - **Operador aritmetico** `*` : _REAL_ (linha 4)
    - **Literal numerico** `3.0` : _REAL_ (linha 4)
    - **Literal numerico** `4.0` : _REAL_ (linha 4)
  - **Operador aritmetico** `|` : _REAL_ (linha 5)
    - **Literal numerico** `10.0` : _REAL_ (linha 5)
    - **Literal numerico** `3.0` : _REAL_ (linha 5)
  - **Operador aritmetico** `/` : _INT_ (linha 6)
    - **Literal numerico** `15` : _INT_ (linha 6)
    - **Literal numerico** `4` : _INT_ (linha 6)
  - **Operador aritmetico** `%` : _INT_ (linha 7)
    - **Literal numerico** `10` : _INT_ (linha 7)
    - **Literal numerico** `3` : _INT_ (linha 7)
  - **Operador aritmetico** `^` : _INT_ (linha 8)
    - **Literal numerico** `2` : _INT_ (linha 8)
    - **Literal numerico** `3` : _INT_ (linha 8)
  - **Definicao de variavel (STORE)** `GLOBAL` : _REAL_ (linha 9)
    - **Literal numerico** `100.0` : _REAL_ (linha 9)
  - **Uso de variavel (LOAD)** `GLOBAL` : _REAL_ (linha 10)
  - **Resultado anterior (RES)** `RES` : _DESCONHECIDO_ (linha 11)
    - **Literal numerico** `0` : _INT_ (linha 11)
  - **Decisao (IFELSE)** `IFELSE` : _DESCONHECIDO_ (linha 12)
    - **Operador relacional** `==` : _BOOL_ (linha 12)
      - **Literal numerico** `10` : _INT_ (linha 12)
      - **Literal numerico** `10` : _INT_ (linha 12)
    - **Resultado anterior (RES)** `RES` : _DESCONHECIDO_ (linha 12)
      - **Literal numerico** `1` : _INT_ (linha 12)
    - **Resultado anterior (RES)** `RES` : _DESCONHECIDO_ (linha 12)
      - **Literal numerico** `0` : _INT_ (linha 12)
  - **Definicao de variavel (STORE)** `CONTADOR` : _INT_ (linha 13)
    - **Literal numerico** `0` : _INT_ (linha 13)
  - **Repeticao (WHILE)** `WHILE` : _INT_ (linha 14)
    - **Operador relacional** `<=` : _BOOL_ (linha 14)
      - **Uso de variavel (LOAD)** `CONTADOR` : _INT_ (linha 14)
      - **Literal numerico** `5` : _INT_ (linha 14)
    - **Definicao de variavel (STORE)** `CONTADOR` : _INT_ (linha 14)
      - **Operador aritmetico** `+` : _INT_ (linha 14)
        - **Uso de variavel (LOAD)** `CONTADOR` : _INT_ (linha 14)
        - **Literal numerico** `1` : _INT_ (linha 14)
  - **Uso de variavel (LOAD)** `CONTADOR` : _INT_ (linha 15)
  - **Operador aritmetico** `|` : _REAL_ (linha 16)
    - **Operador aritmetico** `*` : _REAL_ (linha 16)
      - **Literal numerico** `10.0` : _REAL_ (linha 16)
      - **Literal numerico** `10.0` : _REAL_ (linha 16)
    - **Literal numerico** `2.0` : _REAL_ (linha 16)
